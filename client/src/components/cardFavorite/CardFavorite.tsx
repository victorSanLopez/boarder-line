export default function CardFavorite() {
  // const listedLike = JSON.parse(localStorage.getItem("favorites")) || [];
  // console.log(listedLike);
  return (
    <></>
    // <section>
    //   {listedLike.map((a: any, index) => (
    //     <h2 key={index}>{a}</h2>
    //   ))}
    //   <button type="submit">Effacer les favoris</button>
    // </section>
  );
}
